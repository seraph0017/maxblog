#!/usr/bin/env python
#encoding:utf-8
"""
accounts.urls
~~~~~~~~~~~~~~~~~~~~
用户模块的urls文件 定义所有的urll路径
"""
from django.conf.urls import patterns, url, include

urlpatterns = patterns('',
    # The normal jazz here...
    # (r'^blog/', include('myapp.urls')),
    # url(r'^api/',include(user_resource.urls)),
)